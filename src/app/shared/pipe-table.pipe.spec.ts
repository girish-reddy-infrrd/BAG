import { PipeTablePipe } from './pipe-table.pipe';

describe('PipeTablePipe', () => {
  it('create an instance', () => {
    const pipe = new PipeTablePipe();
    expect(pipe).toBeTruthy();
  });
});
